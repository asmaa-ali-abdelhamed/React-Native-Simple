import * as React from 'react';
import { Text, View, StyleSheet, Image, ScrollView, Icon, Button } from 'react-native';

const Form = () =>{

  
  return(
    <ScrollView>
      
    </ScrollView>
  )}

  
  export default Form;